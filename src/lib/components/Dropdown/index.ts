import { default as Root } from './Root.svelte';
import { default as Item } from './Item.svelte';
import { default as Menu } from './Menu.svelte';
import { default as Trigger } from './Trigger.svelte';
import { default as CheckboxItem } from './CheckboxItem.svelte';

const Dropdown = {
	Root,
	Item,
	Menu,
	Trigger,
	CheckboxItem
};

export default Dropdown;
